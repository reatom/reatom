{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"]
    },
    "test": {
      "dependsOn": ["^build", "testing#build"],
      "outputs": [],
      "passThroughEnv": ["PLAYWRIGHT_*"],
      "inputs": ["src/**/*.tsx", "src/**/*.ts", "test/**/*.ts", "test/**/*.tsx"]
    },
    "publish": {
      "dependsOn": ["build", "test"],
      "outputs": []
    }
  }
}
