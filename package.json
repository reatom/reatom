{
  "name": "reatom-workspace",
  "scripts": {
    "release": "npx tsx ./tools/publish.ts",
    "postinstall": "turbo run build --output-logs=errors-only --filter=./packages/{framework,testing}",
    "build": "turbo run build --output-logs=errors-only --filter=./packages/*",
    "reset": "sh ./tools/reset.sh",
    "test": "turbo run build --output-logs=errors-only --filter=./packages/testing",
    "prettify": "prettier --write */**/**/*.{js,ts,md}",
    "prettify:check": "prettier --check */**/**/*.{js,ts,md}",
    "prettify:watch": "onchange */**/**/*.{js,ts,md} -- prettier --write {{changed}}",
    "package-generator": "tsx tools/new-package.ts",
    "update": "npx npm-check-updates -u",
    "prepare": "husky",
    "actualize-issue-templates": "npx tsx ./tools/actualize-issue-templates.ts"
  },
  "workspaces": [
    "./packages/*"
  ],
  "packageManager": "npm@8.0.0",
  "engines": {
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@commitlint/cli": "^19.2.1",
    "@commitlint/config-conventional": "^19.1.0",
    "yoctobundle": "latest",
    "@types/node": "latest",
    "bun": "^1.1.13",
    "husky": "^9.1.7",
    "prettier": "^3.5.2",
    "tsx": "^4.19.3",
    "turbo": "^2.4.2",
    "typescript": "^5.7.3",
    "uvu": "^0.5.6",
    "vitest": "^3.0.6",
    "zx": "^8.3.2"
  }
}
